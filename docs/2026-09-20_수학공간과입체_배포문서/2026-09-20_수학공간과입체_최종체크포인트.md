# 수학 「공간과 입체」 최종 배포 점검 체크포인트

기록일: 2026-09-20 (2차 갱신). 이번 작업은 문서·패키지 제작만 수행했다. 앱·문항·채점·API·DB·설치 로직은 변경하지 않았다.

## Project

- 프로젝트: 초등학교 6학년 수학 「공간과 입체」
- 앱: `apps/stacking-blocks-math-web`
- GitHub: https://github.com/rlasksk030/classgame
- branch: `feature/spatial-math-redesign-v1`
- local HEAD: `8c2d7d6dea1ea7a34326e1f97530ce8b838df045`
- remote HEAD(FETCH_HEAD, origin/feature/spatial-math-redesign-v1): `8c2d7d6dea1ea7a34326e1f97530ce8b838df045` — local과 완전히 일치. 되돌리거나 앞서갈 필요 없음.
- 1차 문서 작업(이 파일의 이전 판)은 code HEAD `754daf6` 기준이었다. 이번 2차 작업에서 `8c2d7d6`(교사 문제 미리보기 한글 라벨화)까지 반영해 01~05 문서를 다시 대조·재생성했다.
- 시작 시 기존 미추적 QA 폴더·`배포자료/`·preserved.diff를 보존했다. reset/clean/restore를 실행하지 않았다.

## Completion

**교사용 문서 패키지: 제작 완료. 일반 교사 즉시 배포판: PARTIAL. PROJECT COMPLETE 선언 보류.**

최신 코드의 핵심 수업 기능과 기존 QA 근거를 확인했다. 그러나 이를 일반 교사의 무지원 설치 완료와 동일하게 판단할 수 없다.

1. `scripts/installer/runtime.ts`는 TEST 모드만 허용하고 프로젝트 ref 허용목록을 요구한다. 일반 교사의 새 프로젝트는 기본 허용되지 않는다.
2. `src/pages/SetupPage.tsx`에는 일회성 TEST PAT 직접 입력이 남아 있다. 최종 요구인 관리 토큰 복사 없는 권한 연결은 완료되지 않았다.
3. 교사 Auth 계정의 별도 준비가 필요하며, 현재 배포 주소는 TEST다. 일반 교사의 새 계정·새 프로젝트로 제작자 도움 없이 완료한 검증은 없다.
4. 설치 완료 화면의 QR 안내는 링크를 별도 QR 생성기에 넣으라는 방식이다. 자동 QR 제공으로 문서화하지 않았다.

위 4개 항목은 `8c2d7d6`에서도 코드로 직접 재확인했다: `scripts/installer/runtime.ts`는 여전히 `mode: "TEST"`를 하드코딩하고 `INSTALLER_ALLOWED_PROJECT_REFS` 허용목록을 요구하며, `src/pages/SetupPage.tsx`는 여전히 "일회성 TEST 설치 권한 토큰 (PAT)" 수동 입력 필드를 갖는다. 이번 문서 작업 범위에서는 설치 로직을 고치지 않았으므로 상태 불변이다.

기능 구현·문제 콘텐츠·문제은행·핵심 classroom QA는 아래의 검증 범위와 커밋별 근거로 보존한다. 기존 TEST 자동 설치 검증이 일반 교사 설치의 남은 요건을 없애지는 않는다. 이번에 없앤 교사 수동 작업은 0개이며 설치 기능을 수정하지 않았다.

## Recent Real-Use Fixes (누적, 발견 → 현재 해결)

- 빨간 기준 블록 표시: 불명확했음 → `663efd8`/`c5c8ea0`/`d3aa320`로 기준 블록·재료색·WebGL 독립 HTML 표시 추가.
- BLOCK_POSITION 선택: 상호작용 누락이었음 → `663efd8`로 탭 선택과 선택 상태 문구 연결.
- 선택 상태 표시: 위와 동일 커밋으로 해결, 문서 26절에 반영.
- 문제 설명 부족(1차시 CHOICE): 재사용 스텁 문장이었음 → `6b73826`로 실제 데이터 기반 문장 생성 + `problemSemanticAudit.ts` 신설.
- 숫자 증가/감소(7차시 등): 증가만 가능했음(순환) → `754daf6`로 ＋/－ 양방향, 0·9 경계 clamp로 교체.
- 문제 완료 후 다음 문제/다음 단계: 이동 불가였음 → `896b99f`/`e88bceb`로 저장 확인 후 이동 흐름 보완.
- 안내된 탐구 FREE_BUILD 중복: `a2fe86c`로 1~12차시 각 차시 고유 탐구 매핑 확인, 원격 배포 재검증 완료(추가 코드 변경 불필요).
- 교사 화면 내부 영문 problem type: `8c2d7d6`으로 `PROBLEM_TEMPLATE_LABELS` 63개 항목 매핑 추가, 교사 문제 미리보기 드롭다운이 한글 설명으로 표시됨. 04 수업운영가이드에 사용법 절 추가(이번 문서 회차).
- 기타 최신 commit에 포함된 실사용 수정: 위 표와 아래 QA 표에 커밋 단위로 보존.

## iPad Real-Use Findings

아래는 기존 수정 커밋을 대조한 결과다. 이번 문서 작업에서 iPad 실기기를 재시험하지 않았다.

| 발견한 문제 | 현재 해결 상태·근거 |
|---|---|
| BLOCK_POSITION 기준 블록이 불명확 | `663efd8`, `c5c8ea0`, `d3aa320`: 실제 기준 블록, 재료 표시, WebGL 재료와 독립된 HTML 기준 표시 보완 |
| 블록 tap/select 및 선택 상태 | `663efd8`: 탭 선택과 선택 피드백 연결. 문서는 기준 표시와 선택 블록을 구분해 설명 |
| 설명·판단 조건 누락 | `6b73826`: 1차시 CHOICE의 실제 자료에 맞는 문장 생성, semantic audit 추가 |
| 7차시 숫자 증가만 가능 | `754daf6`: ＋/－ 양방향, 0·9 경계 제한, 순환 제거. 4·7·12차시 공통 숫자 격자에 적용 |
| 완료 후 다음 이동 불가 | `896b99f`, `e88bceb`: 완료·서버 저장 확인 후 다음 문제/다음 단계/학습 완료 흐름 보완 |
| 이전 생성 묶음에 옛 문구 유지 | `754daf6` 기록: 기존 묶음은 자동 교체되지 않음. 기존 기록을 삭제하는 방법으로 안내하지 않음 |

## QA 근거와 한계

| 항목 | 확인한 기존 근거 | 판정 범위 |
|---|---|---|
| Independent oracle·문제은행 | `oracle/reports/content-phase-report.md`, `cross-check-report.md` | 당시 고정 30 + 생성 13,920, mismatch 0. 이번 재실행 없음; 이후 의미 수정까지 동일 실행으로 주장하지 않음 |
| semantic QA | `6b73826` 커밋 및 `shared/problemSemanticAudit.ts` | 전 차시 12 seed와 고정 bank 검사 기록. 전 문항의 새로운 수학 증명으로 확대하지 않음 |
| live browser | `qa/browser-lesson9/REPORT.md` | 해당 코드의 19 PASS, 합성 UI/실제 로컬 로직. 원격 성공과 구분 |
| 최신 interaction QA | `754daf6` 커밋 검증 기록 | renderer-mount 5 + live-flow 6 PASS. 숫자 실제 입력·제출 payload 검증 기록 |
| fresh remote | `qa/fresh-e2e-remote/REMOTE_VERIFICATION_RESULT.json` | 2026-09-20 실제 API 6 PASS: 진행·자기평가·친구/기본 정답 누출·12차시·대표 차시 건너뛰기. 최신 모든 화면 재검증은 아님 |
| 30명 controlled QA | `qa/fresh-e2e-remote/LOAD_TEST_30_RESULT.json`, `bef7961` | 합성 학생 30 동시 로그인/진도/목록/작품 성공, 학생·학급 혼선 없음. 30대 실제 기기 실수업 증거가 아님 |
| 9차시 bank/fallback | browser-lesson9 보고 및 fresh remote 누출 검사 | bank 24 연결·fallback 및 공개 응답 보호의 기존 증거 |
| 10→11차시 연속성 | 기존 fresh 설치 사용자 확인 및 관련 저장 검사 | 기존 보고 보존. 이번 브라우저에서 같은 project를 새로 저장·복원하지 않음 |
| Easy Setup | `qa/installer/INSTALLER_REPORT.md` HTTPS 후반 기록 + 사용자 fresh 설치 결과 | HTTPS 세션 검증과 fresh 설치 18/18 보고는 구분. 현재 migration 파일 18개 직접 확인. 일반 교사 배포 완료는 아님 |
| typecheck/lint/build | 최신 `754daf6` 검증 기록 | PASS 기록 확인, 이번 문서 작업에서 재실행 없음 |

과거 보고서의 PARTIAL/NOT_RUN을 무조건 덮어쓰지 않고 이후 증거가 있는 범위만 갱신했다. 위 자료는 해당 실행 시점의 근거이며 이번에 전체 최신 코드를 다시 검증한 결과가 아니다.

## Deployment

- TEST frontend: https://stacking-blocks-math-setup-test.onrender.com/
- setup: https://stacking-blocks-math-setup-test.onrender.com/setup
- teacher: https://stacking-blocks-math-setup-test.onrender.com/teacher
- student: 위 frontend에 교사 화면에서 복사한 학급 링크로 진입. 개인/학급 식별자가 든 링크는 배포물에서 제외.
- installer backend: https://stacking-blocks-math-installer-test.onrender.com
- production 주소를 새로 확정하거나 배포하지 않았다. 위 TEST 주소를 일반 교사 수업용으로 승인한 것이 아니다.

## 최신 UI·문서 대조

1차 작업에서 실제 HTTPS 브라우저로 학생 기본 진입 안내, /setup 단계 표시와 학생 명단·연결 화면, 교사 화면 메뉴를 확인했다. 이번 2차 작업에서는 새 브라우저 세션을 열지 않고, 대신 00~05 문서 6개를 코드 기준으로 다시 대조했다: `SetupPage.tsx`의 `STEP_TITLES`(8단계 라벨), `LessonPage.tsx`/`BlockScene.ts`/`BlockWorld.tsx`의 BLOCK_POSITION 기준 블록·탭 선택 흐름, `ProjectionGrid.tsx`의 ＋/－ clamp 숫자 입력, `TeacherProblemPreview.tsx`+`problemMetadata.ts`의 한글 라벨을 코드에서 직접 읽고 문서 문구와 대조했다. 01/02/03/05는 이미 정확해 그대로 재사용했고, 04에만 교사 문제 미리보기 절을 추가했다.

민감정보 없는 학생 명단 입력 화면 1장을 계속 사용했다(1차 작업에서 캡처, 이번에 재캡처하지 않음). 모든 1~12차시·완료 상태의 최신 브라우저 스크린샷을 확보한 것은 아니다. 해당 범위를 실제 화면 재검증 완료로 기록하지 않는다. 설명서에 병행 redesign 경로의 정책을 라이브 UI 기능으로 넣지 않았다.

## Security

- production changes: NONE. 원격 데이터 쓰기·설정 변경·배포 없음.
- `.dev.vars`, `.env*`, PAT, API key, PIN, 비밀번호, QA runtime 자료는 배포물에 포함하지 않는다. 로컬 키 파일 내용은 읽지 않았다.
- 이번 2차 작업에서 배포 폴더(15개 파일: PDF 6·DOCX 6·README·설치및접속정보 txt·스크린샷) 전체를 PDF 텍스트 추출 + DOCX 내부 XML + txt 원문으로 합쳐 패턴 검사했다. 실제 인증정보·이메일·로컬 경로·PIN 값 0건. 상세는 `2026-09-20_수학공간과입체_문서검수결과.md`.
- 과거 Git 이력 전체가 무결하다는 보증은 하지 않는다.
- 교사 화면 도구 출력에 TEST PIN이 나타났던 사실(1차 작업 기록)은 배포 문서·스크린샷·ZIP에 여전히 미포함이다. 원격 변경 금지 범위이므로 이번에도 재발급하지 않았으며 해당 TEST PIN 재발급은 여전히 필요하다. 실제 값은 재기록하지 않는다.

## Excluded / 배포 전 남은 일

- production 실제 배포 및 일반 교사 설치 권한 연결, 정식 수업 주소 확정.
- 제작자 실시간 도움 없는 신규 교사 설치·학생 첫 저장·재접속의 최종 인수검증.
- 전 차시 최신 화면의 전체 시각 대조, iPad/Safari 전체 실기기 조합 검사.

## Package (2차, 이번 작업)

- folder: `배포자료/2026-09-20_수학공간과입체_교사용배포판/`
- ZIP: `배포자료/2026-09-20_수학공간과입체_교사용배포판.zip` (807036 bytes, sha256 `66e72f665813a6ca2276e882c4fa81c466f7ff15bfa5fe73395ea939832773a5`)
- 15개 파일, PDF 24쪽, testzip CRC PASS, 재추출 후 원본과 `diff -rq` 바이트 비교 PASS, 전체 항목 UTF-8 파일명 플래그 PASS, 민감정보 0건.
- 문서 패키지(00~05 안내서·README·설치및접속정보·DOCX 원본) 제작·검증은 완료다. 아래 PROJECT COMPLETE는 이 문서 패키지가 아니라 "일반 교사 무지원 배포"라는 더 넓은 목표를 가리킨다.

PROJECT COMPLETE: NOT_CONFIRMED — 일반 교사 즉시 배포(무지원 설치) 요건 미충족. 교사용 문서 배포 패키지 자체는 이번 2차 작업으로 완성·검증됨(DISTRIBUTION PACKAGE COMPLETE).
