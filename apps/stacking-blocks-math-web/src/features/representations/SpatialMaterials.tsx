import type { Material } from '../../../shared/problems/contracts/spatial.ts';
import MathGrid from '../answers/MathGrid';
export default function SpatialMaterials({materials}:{materials:Material[]}) {return <div className="spatial-grids">{materials.map((m,i)=>m.kind==='projection'?<MathGrid key={i} face={m.face} title={m.caption} rows={m.grid} readOnly floor={m.face==='top'}/>:(m.kind==='height-map'||m.kind==='partial-height-map')?<MathGrid key={i} title={m.caption} rows={m.grid} numeric readOnly floor/>:m.kind==='layer-map'?m.grids.map((g,j)=><MathGrid key={`${i}-${j}`} title={`${m.caption} ${j+1}층`} rows={g} readOnly floor/>):null)}</div>;}
